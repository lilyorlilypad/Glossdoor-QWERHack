import React from 'react';

const ReviewsTab = ({ companyData }) => {
    // const reviews = companyData.reviews; // Assuming 'reviews' is part of companyData

    return (
        <div>
        {/*//     /!* Render reviews *!/*/}
        {/*//     {reviews.map((review, index) => (*/}
        {/*//         <div key={index}> /!* Review rendering logic *!/</div>*/}
        {/*//     ))}*/}
            review
        </div>
    );
};

export default ReviewsTab;
